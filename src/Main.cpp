import <iostream>;

import DemoBuilder;
import DemoComponent;
import DemoEvent;
import DemoFacade;
import DemoFlyweight;
import DemoFactory;
import DemoMove;
import DemoOrder;
import DemoPrototype;
import DemoThread;

int main()
{
	// DemoBuilder::run();
	DemoComponent::run();
	// DemoEvents::run();
	// DemoFacade::run();
	// DemoFlyweight::run();
	// DemoFactory::run();
	// DemoMove::run();
	// DemoOrders::run();
	// DemoPrototype::run();
	// DemoThread::run();

	std::cin.get();

	return 0;
}

import <iostream>;

import DemoAdapter;
import DemoBuilder;
import DemoComponent;
import DemoEvent;
import DemoFacade;
import DemoFlyweight;
import DemoFactory;
import DemoLambda;
import DemoMove;
import DemoOrder;
import DemoPool;
import DemoPrototype;
import DemoThread;
import DemoView;

int main()
{
	// DemoAdapter::run();
	// DemoBuilder::run();
	// DemoComponent::run();
	// DemoEvents::run();
	// DemoFacade::run();
	// DemoFlyweight::run();
	// DemoFactory::run();
	// DemoMove::run();
	// DemoLambda::run();
	// DemoOrders::run();
	// DemoPool::run();
	// DemoPrototype::run();
	// DemoThread::run();
	// DemoView::run();

	std::cin.get();

	return 0;
}
